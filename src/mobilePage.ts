const mobilePage = (deviceInfo: string) => {

    return (`<div>
        <h3>I am mobile component</h3>
        <small>
            ${deviceInfo}
        </small>
    </div>`);
};

export default mobilePage;