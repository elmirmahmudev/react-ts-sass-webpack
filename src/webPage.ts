const webPage = (deviceInfo: string) => {
    
    return (`<div>
        <h3>I am web component</h3>
        <small>
            ${deviceInfo}
        </small>
    </div>`);
};

export default webPage